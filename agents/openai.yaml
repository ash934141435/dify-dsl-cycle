interface:
  display_name: "Dify DSL Cycle"
  short_description: "Import, run, and iterate Dify DSLs, including remote Docker discovery."
  brand_color: "#1F8A70"
  default_prompt: "Use $dify-dsl-cycle to deploy this Dify DSL. If I provide an SSH target, Docker path, Compose command, and URL, discover the Console API key and workspace automatically."
